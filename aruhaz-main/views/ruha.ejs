<!DOCTYPE html>
<html lang="hu">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js"></script>
    <title>Ruha</title>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <!-- Brand/logo -->
        <a class="navbar-brand" href="/">Főoldal</a>

        <!-- Links -->
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/elelmiszer">Élelmiszer</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/ruha">Ruha</a>
            </li>
            <li class="nav-item float-right">
                <a class="nav-link" href="#">Regisztráció</a>
            </li>
            <li class="nav-item float-right">
                <a class="nav-link" href="#">Belépés</a>
            </li>
        </ul>
    </nav>
    <h1>Ruha osztály</h1>
    <a href="/ruha/feltoltes">Ruha feltöltés</a>
    <div class="container">
        <% for (let i=0; i < robes.length; i++) { %>
            <% if (i % 3==0) { %>
                <div class="row bg-primary text-white m-3">
                    <% } %>
                        <div class="col-sm-4">
                            <h1>Ruha neve: <%= robes[i].fajta %>
                            </h1>
                            <h4>Ruha leírása: <%= robes[i].leiras %>
                            </h4>
                            <p>Ruha ára: <%= robes[i].ar %> Ft
                            </p>
                            <img src="<%= robes[i].kep %>" alt="kep" class="w-100 h-50">
                            <a href="/ruha/<%= robes[i].vonalkod %>" class="text-white">Megjelenítés</a>
                        </div>
                        <% if (i % 3==2) { %>
                </div>
                <% } %>
                    <% }%>
    </div>
</body>

</html>